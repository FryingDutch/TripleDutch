version: "3"

services:
  td_server:
    build: .
    container_name: tripledutch
    ports: 
      - "8001:8001"
  db:
    image: mysql:8.0
    container_name: db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - db:/var/lib/mysql
    ports:
      - '3306:3306'
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpma
    links:
      - db
    environment:
      - PMA_HOST=db
      - PMA_ARBITRARY=1
    restart: always
    ports:
      - 8081:80
volumes:
  db:
    driver: local